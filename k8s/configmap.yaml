apiVersion: v1
kind: ConfigMap
metadata:
  name: sdrf-validator-config
  namespace: sdrf-validator
data:
  LOG_LEVEL: "INFO"
  PORT: "5000"
  WORKERS: "2"
  MAX_FILE_SIZE: "10485760"  # 10MB
  CORS_ORIGINS: "*"
